spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${MY_HOST}:8848
  datasource:
    username: root
    password: 963852
    url: jdbc:mysql://${MY_HOST}:3306/gulimall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: gulimall-ware

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 11000
